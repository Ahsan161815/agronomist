import 'package:flutter/material.dart';

class ShopNow extends StatelessWidget {
  const ShopNow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Show Now Page'));
  }
}
